let age =13;

let maxAge = 100;

let numPerDay = 1;

let totalRequired;
totalRequired = (numPerDay * 365) * (maxAge - age);

const statement1 = "You will need ";
const statement2 = "to last you until the ripe old age of ";
const snack = " skyflakes ";

document.write(statement1 + totalRequired + snack + statement2 + maxAge);